import { String2DatePipe } from './string-2-date.pipe';

describe('String2DatePipe', () => {
  it('create an instance', () => {
    const pipe = new String2DatePipe();
    expect(pipe).toBeTruthy();
  });
});
